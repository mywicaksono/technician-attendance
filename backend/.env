# Local development default (PostgreSQL)
# Change credentials/host/port to your local environment when needed.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/technician_attendance?schema=public"
